#include "wifi_sync.h"

bool WifiSync::startOtaUpdate() {
    // TODO: connect Wi-Fi on demand, run the OTA flow, then disconnect
    // (readme.md #7). Triggered by the Settings screen's "sync now" action
    // or a BLE setting write.
    return false;
}
